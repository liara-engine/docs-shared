/**
 * @file preview_internal.h
 * @brief Not part of the module's surface.
 * @internal
 * `@internal` in a file's own doc block keeps the whole header out of the
 * reference. It travels with the code, which a pattern in a Doxyfile does
 * not: this header can be renamed or moved without anyone remembering to
 * update a list somewhere else.
 */

#ifndef LIARA_PREVIEW_INTERNAL_H
#define LIARA_PREVIEW_INTERNAL_H

/** @brief Documented, and still not published: the file is internal. */
int liara_preview_scratch(void);

#endif
